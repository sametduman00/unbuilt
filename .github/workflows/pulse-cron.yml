name: Pulse Daily

on:
  schedule:
    # Step 1: fetch at 07:15 UTC daily
    - cron: "15 7 * * *"
    # Step 2 rounds: analyze remaining every 30min until done
    - cron: "0 8 * * *"
    - cron: "30 8 * * *"
    - cron: "0 9 * * *"
    - cron: "30 9 * * *"
    - cron: "0 10 * * *"
  workflow_dispatch:

jobs:
  pulse-fetch:
    if: github.event_name == 'workflow_dispatch' || github.event.schedule == '15 7 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Fetch today PH products and save cache
        run: |
          curl -f -X GET \
            "https://www.unbuilt.me/api/pulse/cron" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            -H "User-Agent: GitHub-Actions-Cron" \
            --max-time 55
          echo "Fetch complete"

  pulse-analyze:
    if: github.event_name == 'workflow_dispatch' || github.event.schedule != '15 7 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 28
    steps:
      - name: Analyze all unanalyzed products (batch loop)
        run: |
          MAX=50
          RUN=0
          while [ $RUN -lt $MAX ]; do
            RUN=$((RUN + 1))
            echo "--- Batch $RUN ---"
            RESP=$(curl -s -X GET \
              "https://www.unbuilt.me/api/pulse/analyze-batch" \
              -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
              -H "User-Agent: GitHub-Actions-Cron" \
              --max-time 110)
            echo "$RESP"
            DONE=$(echo "$RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('done',False))" 2>/dev/null || echo "false")
            REM=$(echo "$RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('remaining',999))" 2>/dev/null || echo "999")
            echo "Done=$DONE Remaining=$REM"
            if [ "$DONE" = "True" ] || [ "$REM" = "0" ]; then
              echo "All products analyzed!"
              break
            fi
            sleep 2
          done
